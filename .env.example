# Copy to .env.local. Configure test-mode keys locally; never commit real keys.
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Donations stay disabled unless explicitly set to true.
DONATIONS_ENABLED=false
NEXT_PUBLIC_SITE_URL=http://localhost:3100
